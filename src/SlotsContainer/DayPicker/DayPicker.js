import PropTypes from 'prop-types';

function DayPicker({ dayDiffFromNow }) {
  return (
    <div></div>
  );
}

DayPicker.propTypes = {
  dayDiffFromNow: PropTypes.number
};

export default DayPicker;
