import PropTypes from 'prop-types';

function ReservationListFiltersContainer() {
    return (
        <div></div>
    );
}

ReservationListFiltersContainer.propTypes = {};

export default ReservationListFiltersContainer;
